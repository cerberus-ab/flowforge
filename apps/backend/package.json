{
  "name": "@flowforge/backend",
  "version": "0.1.5",
  "description": "Backend server for FlowForge browser extension",
  "private": true,
  "main": "dist/server.js",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "clean:dist": "rm -rf dist/",
    "clean:data": "rm -rf data/",
    "clean": "npm run clean:dist && npm run clean:data",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "setup": "node scripts/quick-setup.js",
    "build": "npm run clean:dist && tsc -p tsconfig.json && tsc-alias -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "dev": "concurrently \"tsc -p tsconfig.json --watch\" \"tsc-alias -p tsconfig.json --watch\""
  },
  "keywords": [
    "ai",
    "browser",
    "navigation",
    "onboarding",
    "rag",
    "agents",
    "user-flows",
    "user-experience"
  ],
  "dependencies": {
    "@flowforge/contract": "*",
    "@flowforge/page-trail": "*",
    "@lancedb/lancedb": "^0.27.1",
    "@langchain/core": "^1.1.35",
    "@langchain/ollama": "^1.2.6",
    "@langchain/openai": "^1.4.0",
    "@langchain/textsplitters": "^1.0.1",
    "boxen": "^8.0.1",
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "langchain": "^1.2.36",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tsconfig/node22": "^22.0.5",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.6",
    "@types/node": "^25.5.0",
    "concurrently": "^10.0.4",
    "eslint": "^10.8.1",
    "globals": "^17.9.0",
    "tsc-alias": "^1.9.1",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.66.0"
  }
}
